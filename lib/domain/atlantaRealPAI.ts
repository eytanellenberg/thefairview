/**
 * Placeholder file.
 * -----------------
 * Real ESPN-based PAI computation will be reintroduced later
 * once the data contract (GameSummary) is stabilized.
 *
 * This file is intentionally inert to avoid build failures.
 */

export function computeAtlantaRealPAI() {
  return null;
}
